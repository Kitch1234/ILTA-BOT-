print("1. Получили карту")

card_data = get_random_card()

print("2. Получили способность")

ability = get_ability(card_data["id"])

print("3. Генерируем карту")

card_file = await asyncio.wait_for(...)

print("4. Карта сгенерирована")

await add_card(...)

print("5. Сохранили в БД")
