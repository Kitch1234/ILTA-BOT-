async def get_quest_reward(quest_id):

    query = """
    SELECT reward_coins, reward_xp
    FROM quests
    WHERE id = $1;
    """

    async with database.db.acquire() as conn:

        return await conn.fetchrow(
            query,
            quest_id
        )
