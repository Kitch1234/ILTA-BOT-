async def accept_quest(user_id, quest_id):

    print("1. Вошли в accept_quest")

    query = """
    INSERT INTO player_quests (
        user_id,
        quest_id,
        status
    )
    VALUES ($1, $2, 'active');
    """

    print("2. Перед запросом")

    async with db.acquire() as conn:
        await conn.execute(
            query,
            user_id,
            quest_id
        )

    print("3. Запрос выполнен")
